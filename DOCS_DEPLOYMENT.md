# 文檔網站部署指南

本指南說明如何將 MkDocs 文檔部署到 GitHub Pages。

---

## 📊 架構說明

### Repo 結構

```
Taiwan-ICD10-Health-MCP/
├── main 分支                    # 主要分支（原始碼 + 文檔源文件）
│   ├── src/                    # ✅ Python 原始碼
│   ├── docs/                   # ✅ 文檔源文件（使用者會下載）
│   ├── mkdocs.yml              # ✅ MkDocs 配置
│   └── site/                   # ❌ 排除（.gitignore）
│
└── gh-pages 分支               # GitHub Pages 分支（自動生成）
    └── [建置後的靜態網站]      # ❌ 使用者不會下載
```

### 使用者體驗

**當使用者執行 `git clone` 時：**

```bash
git clone https://github.com/yourusername/Taiwan-ICD10-Health-MCP.git

# 下載內容：
✅ src/              # 原始碼
✅ docs/             # 文檔源文件（Markdown）
✅ mkdocs.yml        # 配置文件
❌ site/             # 不下載（.gitignore 排除）
❌ gh-pages 分支     # 不下載（不是預設分支）
```

**為什麼保留 `docs/` 目錄？**

1. ✅ **透明性** - 使用者可以看到文檔源文件
2. ✅ **貢獻性** - 使用者可以修改文檔並提交 PR
3. ✅ **離線查閱** - Markdown 文件可直接在 GitHub 上閱讀
4. ✅ **最佳實踐** - 符合開源專案標準

---

## 🚀 部署步驟

### 1. 安裝 MkDocs

```bash
# 安裝文檔建置依賴
pip install -r requirements-docs.txt
```

### 2. 本地預覽

```bash
# 啟動本地伺服器
mkdocs serve

# 訪問 http://127.0.0.1:8000
```

### 3. 部署到 GitHub Pages

```bash
# 一鍵部署
mkdocs gh-deploy

# 或指定分支
mkdocs gh-deploy --remote-branch gh-pages
```

**這個命令會：**

1. ✅ 建置靜態網站到 `site/` 目錄
2. ✅ 建立或更新 `gh-pages` 分支
3. ✅ 推送到 GitHub
4. ✅ 觸發 GitHub Pages 部署
5. ✅ 清理本地 `site/` 目錄（不提交到 main）

---

## ⚙️ GitHub 設定

### 啟用 GitHub Pages

1. 進入 GitHub Repo → **Settings**
2. 點擊左側 **Pages**
3. 設定：
   - **Source**: Deploy from a branch
   - **Branch**: `gh-pages` / `root`
4. 儲存後，網站將在幾分鐘內發布

### 訪問網站

```
https://yourusername.github.io/Taiwan-ICD10-Health-MCP/
```

---

## 📝 工作流程

### 日常文檔更新

```bash
# 1. 編輯文檔
vim docs/modules/new-feature.md

# 2. 本地預覽
mkdocs serve

# 3. 確認無誤後部署
mkdocs gh-deploy

# 4. 提交文檔源文件到 main 分支
git add docs/
git commit -m "docs: add new feature documentation"
git push origin main
```

### 自動化部署（GitHub Actions）

建立 `.github/workflows/docs.yml`：

```yaml
name: Deploy Documentation

on:
  push:
    branches:
      - main
    paths:
      - 'docs/**'
      - 'mkdocs.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.x

      - name: Install dependencies
        run: |
          pip install -r requirements-docs.txt

      - name: Deploy to GitHub Pages
        run: |
          mkdocs gh-deploy --force
```

這樣每次推送文檔更新到 main 分支時，會自動部署到 GitHub Pages。

---

## 🗂️ 文檔源文件 vs 建置產物

### 保留在 Git（main 分支）

| 文件/目錄 | 說明 | 大小 |
|----------|------|------|
| `docs/` | 文檔源文件（Markdown） | ~100 KB |
| `mkdocs.yml` | 配置文件 | ~5 KB |
| `docs/stylesheets/` | 自訂樣式 | ~2 KB |
| `docs/javascripts/` | 自訂腳本 | ~1 KB |
| `requirements-docs.txt` | 文檔依賴 | ~1 KB |

**總計**: ~110 KB（可忽略不計）

### 排除在 Git（.gitignore）

| 文件/目錄 | 說明 | 大小 |
|----------|------|------|
| `site/` | 建置後的靜態網站 | ~10 MB |
| `.cache/` | MkDocs 快取 | ~1 MB |

**總計**: ~11 MB（已排除）

---

## ✅ 優點總結

### 同一 Repo 的優點

✅ **一致性** - 程式碼和文檔版本同步
✅ **便利性** - 一個 PR 可同時更新程式碼和文檔
✅ **可維護性** - 減少 Repo 數量
✅ **貢獻友好** - 使用者可輕鬆提交文檔改進

### gh-pages 分支的優點

✅ **分離關注點** - 建置產物不污染 main 分支
✅ **自動化** - 建置和部署完全自動化
✅ **不影響複製** - 使用者不會下載 gh-pages 分支
✅ **獨立部署** - 不影響主分支的 Git 歷史

---

## 🔍 常見問題

### Q1: 使用者 clone 會下載文檔源文件嗎？

**A**: 是的，會下載 `docs/` 目錄（~110 KB），但這是**預期行為**：

- ✅ 使用者可以離線查閱文檔（Markdown 可讀性強）
- ✅ 使用者可以貢獻文檔改進
- ✅ 大小可忽略（只有源文件，沒有建置產物）

### Q2: 使用者 clone 會下載建置後的網站嗎？

**A**: **不會**！建置後的 `site/` 目錄：

- ❌ 被 `.gitignore` 排除
- ❌ 只存在於 `gh-pages` 分支（使用者不會下載）

### Q3: 可以完全分離文檔到另一個 Repo 嗎？

**A**: 可以，但**不推薦**：

**優點**:
- 更清晰的分離

**缺點**:
- ❌ 管理複雜（兩個 Repo）
- ❌ 版本同步困難
- ❌ 貢獻門檻提高
- ❌ 不符合開源最佳實踐

---

## 📚 最佳實踐

### 推薦結構

```
Taiwan-ICD10-Health-MCP/
├── README.md               # 簡要說明 + 連結到完整文檔
├── docs/                   # 完整文檔（Markdown 源文件）
│   ├── index.md           # 文檔首頁
│   ├── getting-started.md
│   └── ...
├── mkdocs.yml              # MkDocs 配置
└── src/                    # 原始碼
```

### README.md 連結到文檔

```markdown
# Taiwan ICD10 Health MCP

簡要介紹...

## 📚 文檔

完整文檔請訪問: https://yourusername.github.io/Taiwan-ICD10-Health-MCP/

或查看 [docs/](docs/) 目錄的 Markdown 源文件。
```

---

## 🎯 總結

| 內容 | 位置 | 使用者會下載？ | 大小 |
|------|------|------------|------|
| 原始碼 | main 分支 | ✅ 是 | ~500 KB |
| 文檔源文件 | main 分支 `docs/` | ✅ 是 | ~110 KB |
| 建置產物 | `site/` (本地) | ❌ 否 (.gitignore) | ~10 MB |
| 靜態網站 | gh-pages 分支 | ❌ 否 (不是預設分支) | ~10 MB |

**使用者體驗**:
- Clone 時只下載必要文件（~610 KB）
- 訪問美觀的線上文檔網站
- 可離線查閱 Markdown 源文件
- 可貢獻文檔改進

---

**這是開源專案的標準做法，GitHub、Python、Kubernetes 等都採用這種模式！** ✅
